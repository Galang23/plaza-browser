import React from 'react'
import { Section } from './Section'

export function PerformanceSection(): React.ReactElement {
  return (
    <Section
      id="performance"
      title="Performance"
      citation="Hibernation policy (off / 5min / 15min / 1h) — §3 — lands in a future v1.4.x release."
    />
  )
}
