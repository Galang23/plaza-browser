import React from 'react'
import { Section } from './Section'

export function GeneralSection(): React.ReactElement {
  return (
    <Section
      id="general"
      title="General"
      citation="General preferences (theme, default search engine, startup behavior) coming in a future v1.4.x release."
    />
  )
}
